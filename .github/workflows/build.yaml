---
name: Build

on:
  push:
    branches:
    - actions

jobs:
    test:
      runs-on: ubuntu-22.04
      steps:
      - uses: actions/setup-go@v3
        with:
          go-version: 1.18
      - uses: actions/checkout@v3
      - run: go test ./...
